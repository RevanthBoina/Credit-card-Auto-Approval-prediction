# Web framework (API-only backend; JSON in/out, no server-rendered forms)
Flask==3.0.3

# Machine-learning stack
# Must match (or be compatible with) the scikit-learn version the .pkl was trained with,
# or joblib.load() will fail to unpickle internal sklearn classes.
scikit-learn==1.7.2
xgboost
imbalanced-learn
pandas==2.2.2
numpy==1.26.4

# Model serialisation
joblib==1.4.2

# Environment variable management
python-dotenv==1.0.1
